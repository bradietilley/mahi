node_modules/
dist/
coverage/
.turbo/
.tmp-tests/
.tmp-scaffold/
pnpm-lock.yaml
*.sqlite
*.sqlite3
*.tsbuildinfo

# Prose, not code. Prettier reflows tables to pad every cell to the column
# width and rewrites `*emphasis*` to `_emphasis_`, which is thousands of lines
# of churn in `docs/` for no readability gain. Package READMEs are also
# generated by `scripts/sync-package-metadata.mjs`, so formatting them here
# would just fight that script's output on the next `pnpm metadata`.
*.md
